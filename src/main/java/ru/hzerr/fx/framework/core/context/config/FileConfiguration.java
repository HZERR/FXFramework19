package ru.hzerr.fx.framework.core.context.config;

import org.apache.commons.configuration2.PropertiesConfiguration;

public class FileConfiguration {

    protected final PropertiesConfiguration configuration;

    protected FileConfiguration(PropertiesConfiguration configuration) { this.configuration = configuration; }
}
